<!DOCTYPE html>
<html lang="it">
<%- include('../partials/head', { title: 'Cambia password utente' }) %>
<body class="min-h-screen bg-gradient-to-b from-parchment to-parchment-dark">
  <header class="bg-midnight/95 text-mist py-4 shadow-xl">
    <div class="container mx-auto px-4 flex items-center justify-between">
      <a href="/admin" class="font-title text-xl text-gold hover:text-gold/80 transition-colors">Admin</a>
      <div class="flex items-center gap-4">
        <a href="/admin/users" class="text-mist/70 text-sm">← Torna</a>
      </div>
    </div>
  </header>

  <main class="container mx-auto px-4 py-8">
    <h1 class="font-title text-3xl text-ink mb-4">Cambia password per <%= user.username %></h1>

    <% if (error) { %>
      <div class="mb-4 p-3 bg-blood/10 text-blood rounded"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="mb-4 p-3 bg-green-100 text-green-800 rounded"><%= success %></div>
    <% } %>

    <div class="card p-6 max-w-md">
      <form method="POST" action="/admin/users/<%= user.id %>/password">
        <label class="block mb-2 text-sm text-ink/70">Nuova password</label>
        <input name="new_password" type="password" class="w-full mb-4 input" required />

        <label class="block mb-2 text-sm text-ink/70">Conferma nuova password</label>
        <input name="new_password_confirm" type="password" class="w-full mb-4 input" required />

        <div class="flex justify-end">
          <button class="btn">Aggiorna password</button>
        </div>
      </form>
    </div>
  </main>
</body>
</html>
